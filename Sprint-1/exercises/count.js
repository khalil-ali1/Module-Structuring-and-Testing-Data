let count = 0;

count = count + 1;

// Line 1 is a variable declaration, creating the count variable with an initial value of 0
/* Line 3 is reassignment for the variable count.
   we start from the righthand part so  0 + 1 = 1
   the " = " in line 3 means assigning the righthand part to the lefthand var.  
   count = 1 ;
*/
